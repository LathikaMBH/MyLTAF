{
  "name": "LTAF",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run": "env-cmd -f ./.env cypress run --headless",
    "cy:run:chrome": "env-cmd -f ./.env cypress run --browser chrome",
    "cy:open": "env-cmd -f ./.env cypress open",
    "format": "npx prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "env-cmd": "^10.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "cypress-xpath": "^2.0.1",
    "cypress": "^13.8.0"
  }
}
